export const baseUrl = "https://icarus.fandom.com";

export const benchs: Array<{
  name: string;
  imageUrl: string;
  url: string;
  craft: Array<{ amount: number; title: string; link: string }>;
}> = [];
